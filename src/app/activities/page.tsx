import ComingSoon from "@/components/layout/ComingSoon";
export default function ActivitiesPage() {
  return <ComingSoon module="Activities" description="Full log of every action across your team — emails, meetings, notes, and status changes, all filterable." />;
}
